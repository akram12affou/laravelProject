<header>
    <nav>
        <a href="{{ url('/') }}">Home</a>
        <a href="{{ url('/about') }}">About</a>
       <!-- @auth
             <a >Logout</a>
        @else
            <a >Login</a>
            <a >Register</a>
        @endauth -->
    </nav>
</header>